# 🔐 Password Generator

A clean, cryptographically secure password generator built with pure HTML, CSS, and JavaScript. No frameworks, no dependencies — just open and use.

## ✨ Features

- Cryptographically secure generation using `crypto.getRandomValues()`
- Adjustable password length (8 to 64 characters)
- Toggle character sets: uppercase, lowercase, numbers, symbols
- Option to exclude ambiguous characters (0, O, l, 1, I)
- Live strength meter based on entropy calculation
- One-click copy to clipboard
- Dark mode support (auto-detects system preference)
- Zero dependencies — works offline

## 🚀 Usage

No installation needed. Just open `password_generator.html` in any modern browser.

```bash
# Clone the repo
git clone https://github.com/RizCodes2/Random Password Generator.git

# Open the file
open index.html
```

## 🛠️ Built With

- HTML5
- CSS3 (CSS Variables, Grid, Flexbox)
- Vanilla JavaScript (Web Crypto API)
- [Syne](https://fonts.google.com/specimen/Syne) & [Space Mono](https://fonts.google.com/specimen/Space+Mono) — Google Fonts

## 📁 Project Structure

```
Random Password Generator/
└── index.html   
└── style.css
└── app.js
└── README.md
```

## 📄 License

Free to use, modify, and distribute.
